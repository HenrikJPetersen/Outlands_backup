if maxweight = 390
    overhead '390'
    if weight > 393
        overhead 'true'
    endif
elseif maxweight = 460
    overhead '460'

    if weight > 403
        overhead 'weight equals 400'
    endif
endif