if skill 'archery' > 79
    if findtype 'bow' backpack as bow
        dclick bow
    endif
endif

pause 150
if cooldown poisonstrike = 0
    @setvar! globalcooldown 1
    say '[poisonstrike'
else
    say '[poisonstrike'
    pause 150
    if insysmsg 'You must wait'
        @setvar! globalcooldown 0
        stop
    endif    
    @setvar! globalcooldown 1
endif
clearsysmsg 